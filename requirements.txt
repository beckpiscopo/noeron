# pyproject.toml is the authoritative source of truth for dependencies.
# This file is a convenience copy for tools that require requirements.txt (e.g. Railway).
# Keep in sync with pyproject.toml [project.dependencies].

# Core dependencies for production deployment
fastapi>=0.104.0
fastmcp>=0.2.0
mcp>=1.0.0
uvicorn>=0.24.0
pydantic>=2.0.0
httpx>=0.27.0
python-dotenv>=1.0.0

# AI/ML
google-genai>=1.60.0
# sentence-transformers removed - production uses Gemini text-embedding-004

# Database
supabase>=2.0.0
# chromadb>=0.4.0  # Only needed for local dev, production uses Supabase pgvector

# Text processing
tiktoken>=0.5.0
beautifulsoup4>=4.12.0
pypdf>=4.0.0

# Utilities
numpy>=2.0
arxiv>=2.1.0

# Slide deck PDF generation
pillow>=12.1.0
reportlab>=4.4.9

# Optional: Transcript generation (not needed for production runtime)
# yt-dlp
# assemblyai
# ffmpeg
